"use strict";
let expect = require('chai').expect;
let charactersBetween = require('../src/charactersBetween.js');

describe('charactersBetween', () => {
  it('exists as a function', function () {
    expect(typeof charactersBetween).to.equal('function');
  });
});//end describe('bookStorageFormat'
































