'use strict';
const _ = require('lodash');
const preSnippetClassify = require('./preSnippetClassify');


/*
* extendedPreSnippets -> [['a'], ['c', 'd']]
* */
const classifyPreSnippetArrangement = (extendedPreSnippets) => {

};

module.exports = classifyPreSnippetArrangement;